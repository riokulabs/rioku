// Plan 06 (notifications) feature slice — owned by parallel plan that retires notification-routing mocks.
// Currently no slice-specific exports; the centralized store lives in './_internal'.
export {};
