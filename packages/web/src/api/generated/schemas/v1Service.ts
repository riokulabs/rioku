/**
 * Generated by orval v7.5.0 🍺
 * Do not edit manually.
 * Rioku API
 * Rioku is an open-source, AI-native API gateway platform built on
Caddy. This document is the canonical OpenAPI 3.1 specification
for every REST endpoint exposed by the daemon.

Conventions:

- Tenant-scoped paths live under `/api/v1/t/{tenant}/...`.
- Every resource exposes `OPTIONS` for capability discovery.
- Both `PUT` (replace, full body) and `PATCH` (partial, RFC 7396
  merge-patch) are supported on every resource that takes
  mutations.
- Relations carry both the foreign-key id AND a `_links.<relation>.href`
  pointing at the canonical resource path.
- Pagination is cursor-based (`?page_token=`); page size is
  bounded at 1000.

 * OpenAPI spec version: 0.1.0
 */
import type { V1Compression } from './v1Compression';
import type { V1ConnectionPool } from './v1ConnectionPool';
import type { V1HealthCheck } from './v1HealthCheck';
import type { V1Labels } from './v1Labels';
import type { V1LoadBalancingPolicy } from './v1LoadBalancingPolicy';
import type { V1PassiveHealthCheck } from './v1PassiveHealthCheck';
import type { V1RequestHeaders } from './v1RequestHeaders';
import type { V1ResponseHeaders } from './v1ResponseHeaders';
import type { V1ResponseRule } from './v1ResponseRule';
import type { V1RetryPolicy } from './v1RetryPolicy';
import type { V1UpstreamTLS } from './v1UpstreamTLS';
import type { V1Upstream } from './v1Upstream';

export interface V1Service {
  /** compression configures gzip/zstd encoding of upstream responses.
Compiles into Caddy's encode handler positioned before reverse_proxy.
When unset or disabled, no encoding handler is emitted. */
  compression?: V1Compression;
  connectionPool?: V1ConnectionPool;
  createdAt?: string;
  dialTimeoutSeconds?: number;
  healthCheck?: V1HealthCheck;
  id?: string;
  idleTimeoutSeconds?: number;
  labels?: V1Labels;
  /** lb_cookie_name is the cookie used by LB_POLICY_COOKIE for sticky
sessions. Empty defaults to "lb". Ignored for non-cookie policies. */
  lbCookieName?: string;
  /** lb_header_name is the request header consulted by LB_POLICY_HEADER
to pick an upstream (consistent hash on the header value). Required
when lb_policy = LB_POLICY_HEADER; ignored otherwise. */
  lbHeaderName?: string;
  lbPolicy?: V1LoadBalancingPolicy;
  name?: string;
  passiveHealthCheck?: V1PassiveHealthCheck;
  /** request_headers mutates request headers before proxying to the
upstream. Compiles into a Caddy headers handler positioned before
reverse_proxy in the route's handler chain. */
  requestHeaders?: V1RequestHeaders;
  responseHeaderTimeoutSeconds?: number;
  /** response_headers mutates response headers after receiving from the
upstream. Compiled into reverse_proxy.headers.response. */
  responseHeaders?: V1ResponseHeaders;
  /** response_rules is a list of status-code-conditional rules that
determine how the gateway handles specific upstream responses.
Compiled into reverse_proxy.handle_response. */
  responseRules?: V1ResponseRule[];
  retryPolicy?: V1RetryPolicy;
  updatedAt?: string;
  upstreamTls?: V1UpstreamTLS;
  upstreams?: V1Upstream[];
}
