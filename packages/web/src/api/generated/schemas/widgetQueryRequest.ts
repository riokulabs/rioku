// @ts-nocheck
/**
 * Generated by orval v7.5.0 🍺
 * Do not edit manually.
 * Rioku API
 * Rioku is an open-source, AI-native API gateway platform built on
Caddy. This document is the canonical OpenAPI 3.1 specification
for every REST endpoint exposed by the daemon.

Conventions:

- Tenant-scoped paths live under `/api/v1/t/{tenant}/...`.
- Every resource exposes `OPTIONS` for capability discovery.
- Both `PUT` (replace, full body) and `PATCH` (partial, RFC 7396
  merge-patch) are supported on every resource that takes
  mutations.
- Relations carry both the foreign-key id AND a `_links.<relation>.href`
  pointing at the canonical resource path.
- Pagination is cursor-based (`?page_token=`); page size is
  bounded at 1000.

 * OpenAPI spec version: 0.1.0
 */
import type { WidgetQueryRequestType } from './widgetQueryRequestType';

/**
 * Dashboard widget query body. The daemon dispatches to the
Prometheus endpoint matching `type`:

- `instant` → `/api/v1/query` (uses `expr`, optional `time`)
- `range`   → `/api/v1/query_range` (uses `expr`, `start`, `end`, `step`)
- `series`  → `/api/v1/series` (uses `match[]`, optional `start`/`end`)

Tenant isolation is enforced at the AST layer — every
VectorSelector / `match[]` selector is rewritten with
`tenant_id="<tenant>"` and selected bypass patterns
(label_replace targeting tenant_id, on/ignoring with tenant_id,
regex `__name__` matchers, mismatched explicit tenant_id) are
rejected with HTTP 400.

 */
export interface WidgetQueryRequest {
  /** Range/series query window end. */
  end?: string;
  /** PromQL expression for instant/range queries. */
  expr?: string;
  /** Series query match[] selectors. */
  match?: string[];
  /** Range/series query window start. */
  start?: string;
  /** Range query step (e.g. `30s`, `1m`). */
  step?: string;
  /** Instant query timestamp (Prometheus-formatted). */
  time?: string;
  type: WidgetQueryRequestType;
}
