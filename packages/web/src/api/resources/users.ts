// Types for the users resource surface.
// User is defined in common.ts (shared with many features); no additional user-specific types here yet.

export {};
// TODO: Add user-specific request/response types when real endpoints are wired.
