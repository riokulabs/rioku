// Package caddy implements the Caddy admin API client and child process manager.
// The daemon owns Caddy's full lifecycle: start, stop, config push, and
// zero-downtime binary swap on plugin rebuild.
package caddy
