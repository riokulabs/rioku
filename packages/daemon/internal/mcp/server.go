// Package mcp implements the MCP (Model Context Protocol) server that
// exposes rioku capabilities as tools that AI agents can call.
// Runs inside the daemon, exposed via the REST gateway at /mcp/v1/.
package mcp
