// Package agentidentity implements first-class identity primitives for
// AI agents. API key issuance, scoped permissions, session tracking,
// and hierarchical agent identity (sub-agents).
package agentidentity
